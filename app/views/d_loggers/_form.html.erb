<%= form_for(@d_logger) do |f| %>
  <% if @d_logger.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@d_logger.errors.count, "error") %> prohibited this d_logger from being saved:</h2>

      <ul>
      <% @d_logger.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :id %><br />
    <%= f.text_field :id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
