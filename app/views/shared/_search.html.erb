<%= form_for :search, :url => { :controller=>'product_instances', :action => 'index' }, :html => {:class => 'form-horizontal' }, :remote=>true do |f| %>
<div style="padding: 10px"> 
	<label style="font-weight: bold"> Search In</label>
	<%= select_tag "", ""%>
	<div class="clearfix"></div>
	<br />
	<label style="font-weight: bold"> Search For</label>
	<%= text_field_tag "", "", :placeholder => 'Type in to search...' %>

	<br />
	<br />
	<%= submit_tag "Search" , :class => 'btn btn-success', :remote => true, "data-disable-with" => "Searching..." %> 
	<%= link_to "Advanced Search", "#", :class => 'btn btn-success', :remote => true %> 
	<%= link_to_function "Cancel", "$('#modalPopup').dialog('close')", :class => 'btn btn-success' %> 
</div>
<% end %>
